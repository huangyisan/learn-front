如果是父子关系, 则子元素会层叠在父元素上

如果不是父子关系:
1. 都是非定位元素,则在标准流中一般不会存在层叠现象
2. 一个是定位元素,一个是非定位元素,那么定位元素会层叠在非定位元素上面
3. 两个都是定位元素,则根据css属性z-index来控制层叠顺序,比较方式,如果是兄弟元素,数字越大,层叠越放上;如果相等,则后面的兄弟元素层叠在前面的兄弟元素上;
                   如果不是兄弟元素,则各自从元素自己以及祖先元素中,找出最邻近2个定位元素进行比较.


当加入浮动元素(float)之后, 定位元素会层叠在浮动元素上面